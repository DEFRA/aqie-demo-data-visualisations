
# Base URL of the aqie-back-end service providing /measurements
# Must be set to the CDP-internal address when deployed; localhost only works locally
AQIE_BACK_END_URL=http://localhost:3001

# Overrides the DEFRA SOS feed base URL if needed
#SOS_URL=

# Set by the CDP platform; outbound internet calls are routed through it
#CDP_HTTPS_PROXY=

# Password for basic authentication (only enforced when running in production mode)
PASSWORD=
